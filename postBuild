set -eux
python setup.py develop

git clone https://github.com/deathbeds/wxyz

pushd wxyz

set -eux
python -m pip install -e ./packages/ipywxyz --ignore-installed --no-dependencies

jlpm bootstrap
jupyter labextension install $(cat labex.txt) --no-build
jupyter lab build

popd